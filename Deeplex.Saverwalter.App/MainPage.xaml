<Page
    x:Class="Deeplex.Saverwalter.App.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Deeplex.Saverwalter.App"
    xmlns:mtuuc="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:vm="using:Deeplex.Saverwalter.App.ViewModels"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid
        x:Name="LayoutRoot"
        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <NavigationView
            PaneDisplayMode="Left"
            x:Name="NavView"
            OpenPaneLength="160"
            AlwaysShowHeader="False"
            IsSettingsVisible="True"
            ItemInvoked="NavView_ItemInvoked"
            BackRequested="NavView_BackRequested"
            IsBackEnabled="{x:Bind AppFrame.CanGoBack, Mode=OneWay}">

            <NavigationView.MenuItems>
                <NavigationViewItem
                    x:Name="KontaktListMenuItem"
                    Content="{x:Bind KontaktListLabel}"
                    Icon="ContactInfo"/>
                <NavigationViewItem
                    x:Name="WohnungListMenuItem"
                    Content="{x:Bind WohnungListLabel}" 
                    Icon="Street"/>
                <NavigationViewItem
                    x:Name="VertragListMenuItem"
                    Content="{x:Bind VertragListLabel}" 
                    Icon="Library"/>
                <NavigationViewItem
                    x:Name="BetriebskostenListMenuItem"
                    Content="{x:Bind BetriebskostenrechnungenListLabel}" 
                    Icon="List"/>
            </NavigationView.MenuItems>

            <NavigationView.PaneFooter>
                <StackPanel>
                    <!--TODO about?-->
                </StackPanel>
            </NavigationView.PaneFooter>

            <SplitView
                x:Name="splitview"
                DisplayMode="CompactInline"
                PanePlacement="Right"
                OpenPaneLength="300"
                IsPaneOpen="True">
                <SplitView.Content>
                <Frame
                x:Name="frame"
                Margin="0,0,0,0">
                    <Frame.ContentTransitions>
                        <TransitionCollection>
                            <NavigationThemeTransition>
                                <NavigationThemeTransition.DefaultNavigationTransitionInfo>
                                    <EntranceNavigationTransitionInfo />
                                </NavigationThemeTransition.DefaultNavigationTransitionInfo>
                            </NavigationThemeTransition>
                        </TransitionCollection>
                    </Frame.ContentTransitions>
                </Frame>
                </SplitView.Content>
                <SplitView.Pane>
                    <StackPanel>
                        <RelativePanel>
                            <TextBlock Margin="5,0,0,0" Style="{StaticResource SubtitleTextBlockStyle}" Text="Explorer" RelativePanel.AlignLeftWithPanel="True"/>
                            <ToggleButton
                                RelativePanel.AlignRightWithPanel="True"
                                Width="50"
                                CornerRadius="0"
                                Background="Transparent"
                                x:Name="togglepane"
                                Click="togglepane_Click">
                                <SymbolIcon x:Name="togglepanesymbol" Symbol="OpenPane"/>
                            </ToggleButton>
                        </RelativePanel>
                        <mtuuc:WrapPanel Orientation="Horizontal" Margin="2.5,0,2.5,0">
                            <Button CornerRadius="0">
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal">
                                        <SymbolIcon Symbol="ContactInfo"/>
                                        <TextBlock Text=" Personen"/>
                                    </StackPanel>
                                </Button.Content>
                                <Button.Flyout>
                                    <Flyout>
                                        <muxc:ItemsRepeater ItemsSource="{x:Bind ViewModel.Anhang.AnhangKontaktList.Value}">
                                            <DataTemplate x:DataType="vm:AnhangKontakt">
                                                <MenuFlyoutItem Text="{x:Bind Bezeichnung}"/>
                                            </DataTemplate>
                                        </muxc:ItemsRepeater>
                                    </Flyout>
                                </Button.Flyout>
                            </Button>
                            <Button CornerRadius="0">
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal">
                                        <SymbolIcon Symbol="Street"/>
                                        <TextBlock Text=" Adressen"/>
                                    </StackPanel>
                                </Button.Content>
                                <Button.Flyout>
                                    <Flyout>
                                        <muxc:ItemsRepeater ItemsSource="{x:Bind ViewModel.Anhang.AnhangAdresseList.Value}">
                                            <DataTemplate x:DataType="vm:AnhangAdresse">
                                                <MenuFlyoutItem Text="{x:Bind Bezeichnung}"/>
                                            </DataTemplate>
                                        </muxc:ItemsRepeater>
                                    </Flyout>
                                </Button.Flyout>
                            </Button>
                            <Button CornerRadius="0">
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal">
                                        <SymbolIcon Symbol="Library"/>
                                        <TextBlock Text=" Verträge"/>
                                    </StackPanel>
                                </Button.Content>
                                <Button.Flyout>
                                    <Flyout>
                                        <muxc:ItemsRepeater ItemsSource="{x:Bind ViewModel.Anhang.AnhangVertragList.Value}">
                                            <DataTemplate x:DataType="vm:AnhangVertrag">
                                                <MenuFlyoutItem Text="{x:Bind Bezeichnung}"/>
                                            </DataTemplate>
                                        </muxc:ItemsRepeater>
                                    </Flyout>
                                </Button.Flyout>
                            </Button>
                        </mtuuc:WrapPanel>
                        <TextBlock Text="{x:Bind ViewModel.Anhang.FilterText.Value}"/>
                        <ListView ItemsSource="{x:Bind ViewModel.Anhang.Dateien.Value}">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="vm:AnhangDatei">
                                    <TextBlock Text="{x:Bind DateiName}"/>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </SplitView.Pane>
            </SplitView>
        </NavigationView>
    </Grid>
</Page>
