<UserControl
    x:Class="Deeplex.Saverwalter.App.UserControls.AddZaehlerstandControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Deeplex.Saverwalter.App.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Button x:Name="AddZaehlerButton" Content="Zählerstand hinzufügen">
            <Button.Flyout>
                <Flyout>
                    <StackPanel Orientation="Horizontal">
                        <CalendarDatePicker
                            Date="{x:Bind Datum, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                            DateFormat="{}{day.integer}.{month.integer}.{year.full}"
                            Header="Datum" />
                        <TextBox
                            Text="{x:Bind ViewModel.Stand, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                            Header="Stand"/>
                        <Button VerticalAlignment="Bottom" Click="AddZaehlerstand_Click" Content="Bestätigen" />
                    </StackPanel>
                </Flyout>
            </Button.Flyout>
        </Button>
    </Grid>
</UserControl>
