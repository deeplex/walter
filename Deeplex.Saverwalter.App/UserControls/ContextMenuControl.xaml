<UserControl
    x:Class="Deeplex.Saverwalter.App.UserControls.ContextMenuControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Deeplex.Saverwalter.App.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel>
        <Button
        Visibility="{x:Bind Anhang}"
        Background="Transparent"
        Click="Anhaenge_Click">
            <StackPanel Orientation="Horizontal">
                <SymbolIcon Symbol="Attach"/>
                <TextBlock Margin="10,0,0,0" Text="Anhänge anzeigen" />
            </StackPanel>
        </Button>
        <Button
        Visibility="{x:Bind Navigation}"
        Background="Transparent"
        Click="Navigation_Click">
            <StackPanel Orientation="Horizontal">
                <SymbolIcon Symbol="Forward"/>
                <TextBlock HorizontalAlignment="Stretch" Margin="10,0,0,0" Text="Details anzeigen" />
            </StackPanel>
        </Button>
    </StackPanel>
</UserControl>
