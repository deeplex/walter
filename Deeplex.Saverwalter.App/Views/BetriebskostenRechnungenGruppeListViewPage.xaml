<Page
    x:Class="Deeplex.Saverwalter.App.Views.BetriebskostenRechnungenGruppeListViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Deeplex.Saverwalter.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mtuuc="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:vm="using:Deeplex.Saverwalter.App.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <muxc:StackLayout x:Name="VerticalStackLayout" Orientation="Vertical" Spacing="8"/>
    </Page.Resources>

    <StackPanel>
        <CommandBar
            Background="Transparent"
            IsOpen="False"
            HorizontalAlignment="Stretch"
            DefaultLabelPosition="Right">
            <CommandBar.Content>
                <TextBlock Text="Betriebskostenrechnungen"
                    Style="{StaticResource TitleTextBlockStyle}"
                    Margin="10,0,0,0"/>
            </CommandBar.Content>
            <AppBarButton Icon="Sync" Label="Sortiere nach Typ" Click="SortiereNachTyp_Click"/>
        </CommandBar>

        <muxc:ItemsRepeater
            Layout="{StaticResource VerticalStackLayout}"
            ItemsSource="{x:Bind ViewModel.Gruppen.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <DataTemplate>
                <Grid>
                    <mtuuc:Expander Header="{Binding Key.Bezeichnung}">
                        <StackPanel>
                            <Pivot ItemsSource="{Binding Value.Jahre, Mode=OneWay}">
                                <Pivot.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Key, Mode=OneWay}"/>
                                    </DataTemplate>
                                </Pivot.HeaderTemplate>
                                <Pivot.ItemTemplate>
                                    <DataTemplate>
                                        <GridView Margin="0,10,0,0" ItemsSource="{Binding Value, Mode=OneWay}">
                                            <CollectionViewSource x:Name="Wohnungen" IsSourceGrouped="True"/>
                                            <GridView.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel BorderBrush="LightGray" BorderThickness="2" Padding="5">
                                                        <TextBlock
                                                        Margin="5"
                                                        FontSize="18">
                                                        <Run Text="Betrag:"/>
                                                        <Run Text="{Binding Betrag}"/>
                                                        </TextBlock>
                                                        <TextBlock
                                                        Margin="5"
                                                        FontSize="18">
                                                        <Run Text="Datum:"/>
                                                        <Run Text="{Binding Datum}"/>
                                                        </TextBlock>
                                                        <TextBlock
                                                            Margin="5"
                                                            FontSize="18"
                                                            Text="{Binding Typ}"/>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </GridView.ItemTemplate>
                                        </GridView>
                                    </DataTemplate>
                                </Pivot.ItemTemplate>
                            </Pivot>
                        </StackPanel>
                    </mtuuc:Expander>
                </Grid>
            </DataTemplate>
        </muxc:ItemsRepeater>

    </StackPanel>
</Page>
