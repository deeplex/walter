<Page
    x:Class="Deeplex.Saverwalter.App.Views.BetriebskostenrechnungenDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Deeplex.Saverwalter.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mtuuc="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
    <mtuuc:WrapPanel>
        <StackPanel>
            <!--TODO der Button kommt in die AppBar-->
            <Button
                    Margin="10,0,0,0"
                    VerticalAlignment="Bottom">
                <SymbolIcon Symbol="Attach"/>
            </Button>
            <TextBlock Text="Betroffene Wohneinheiten:"/>
            <muxc:TreeView
                SelectionMode="Multiple"
                x:Name="WohnungenTree"/>
            <MenuFlyoutSeparator/>
            <TextBox Header="Beschreibung"/>
            <StackPanel
                Margin="0,10,0,0"
                Orientation="Horizontal">
                <ComboBox Header="Kostentyp"/>
                <ComboBox
                    Margin="10,0,0,0"
                    Header="Umlageschlüssel"/>
                <muxc:NumberBox
                    Margin="10,0,0,0"
                    Header="Betr. Jahr"
                    SpinButtonPlacementMode="Inline"/>
            </StackPanel>
            <StackPanel
                Margin="0,10,0,0"
                Orientation="Horizontal">
                <CalendarDatePicker Header="Datum"/>
                <muxc:NumberBox Margin="10,0,0,0" Header="Betrag"/>
            </StackPanel>
            <!--{x:Bind ViewModel.isHeizung}-->
            <StackPanel Margin="0,10,0,0" Visibility="Visible" Orientation="Horizontal">
                <Slider
                    Width="100"
                    Minimum="50"
                    Maximum="70"
                    Header="HKVO§7"/>
                <Slider
                    Width="100"
                    Minimum="50"
                    Maximum="70"
                    Margin="10,0,0,0"
                    Header="HKVO§8"/>
                <ComboBox
                    Margin="10,0,0,0"
                    Header="HKVO§9"/>
            </StackPanel>
            <!--if heizkosten...-->
        </StackPanel>
    </mtuuc:WrapPanel>
    </ScrollViewer>
</Page>
