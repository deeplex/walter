<UserControl
    x:Class="Deeplex.Saverwalter.WinUI3.AddPersonControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Deeplex.Saverwalter.WinUI3"
    xmlns:uc="using:Deeplex.Saverwalter.WinUI3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mtuuc="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Button
        Margin="20,0,0,0"
        Background="Transparent">
        <SymbolIcon Symbol="Add"/>
        <Button.Flyout>
            <Flyout>
                <StackPanel Orientation="Horizontal">
                    <uc:ComboControl
                        Header="Person"
                        Margin="20,20,0,0"
                        PlaceholderText="Füge Person hinzu..."
                        SelectedItem="{x:Bind ViewModel.Selected, Mode=TwoWay}"
                        ItemsSource="{x:Bind ViewModel.List.Value, Mode=OneWay}"/>
                    <Button
                        Margin="0,30,0,0"
                        Background="Transparent"
                        Command="{x:Bind ViewModel.Add}">
                        <SymbolIcon Symbol="Add"/>
                    </Button>
                </StackPanel>
            </Flyout>
        </Button.Flyout>
    </Button>
</UserControl>
